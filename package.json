{
  "name": "roomos-root",
  "private": true,
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "node-server:dev": "node node-server/server.js",
    "dev": "npm run node-server:dev & npm run frontend:dev",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
